# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema
# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# User name for the PostgreSQL database (see docker-compose.yml)
POSTGRES_USER=admin

# Password for the PostgreSQL database (see docker-compose.yml)
POSTGRES_PASSWORD=admin

# Hostname for the PostgreSQL database
POSTGRES_HOST=localhost

# Port number for the PostgreSQL database (see docker-compose.yml)
POSTGRES_PORT=5432

# Name of the PostgreSQL database to be used (see docker-compose.yml)
POSTGRES_DB=pet-radar-DB

# Full database connection URL constructed from the above PostgreSQL variables used for Prisma
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}